'use strict';

var _ = require('./../');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
 |----------------------------------------------------------------
 | Shell Commands
 |----------------------------------------------------------------
 |
 | Need to execute a shell script, as part of your compile
 | process? No problem. Flixir can help with that. Just
 | call `mix.exec('command')`, and, bam, you're set!
 |
 */

_2.default.extend('exec', function (command, watcher) {
    var task = new _2.default.Task('exec', function (gulp, $) {
        _2.default.Log.heading('Triggering Command...').message(command);

        return gulp.src('').pipe(_2.default.Plugins.shell(command));
    });

    if (watcher) {
        task.watch(watcher);
    }
});